server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LPVictoria9/config-dataVictoria
          clone-on-start: true
          bootstrap: true
          enabled: true
          username:  {GET_USER}
          password:  {GET_PWS}
          search-paths: config-data
          default-label: master

  security:
    user:
      name: root
      password: s3cr3t
